import { StyleSheet } from "react-native";
import _ from '../../component/function';
export default StyleSheet.create({
  container: {
    flex: 1,
  },
  view:{
    backgroundColor:'red',
    width:_.wp(50),
    height:_.wp(20),
  }
});